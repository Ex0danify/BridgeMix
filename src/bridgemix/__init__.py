"""Roland Bridge Cast UI — Linux controller for the Bridge Cast USB audio mixer."""

# The Release workflow (.github/workflows/release.yml) rewrites this literal on every release.
__version__ = "1.2.1"
